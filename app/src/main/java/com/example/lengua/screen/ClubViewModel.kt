package com.example.lengua.screen

import androidx.lifecycle.ViewModel

// Archivo neutralizado para eliminar errores de compilación.
class ClubViewModel : ViewModel() {
    // El contenido se eliminó porque causaba errores de compilación.
}
